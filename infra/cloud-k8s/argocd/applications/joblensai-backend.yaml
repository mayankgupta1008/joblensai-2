apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: joblensai-backend
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: joblensai
  source:
    repoURL: https://github.com/mayankgupta1008/joblensai-2
    targetRevision: main
    path: apps/backend/chart
    helm:
      valueFiles:
        - values-local.yaml  # Change to values-prod.yaml for AWS

  destination:
    server: https://kubernetes.default.svc
    namespace: joblensai
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

JobLens AI Backend Service deployed!

Service: {{ include "api-gateway.fullname" . }}
Namespace: {{ .Release.Namespace }}

Quick access:
  kubectl port-forward svc/{{ include "api-gateway.fullname" . }} 5001:{{ .Values.service.port }} -n {{ .Release.Namespace }}
  curl http://localhost:5001/api/backend/health

View logs:
  kubectl logs -l app.kubernetes.io/name={{ include "api-gateway.name" . }} -n {{ .Release.Namespace }} -f
